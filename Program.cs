namespace PlayerBoardGame
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("===========================================");
            Console.WriteLine("║   Welcome to the 2-PLAYER BOARD GAME !  ║");
            Console.WriteLine("===========================================");

            // 1. Ask for Game Type

            // 2. Ask for Game Mode

            // 3. Creates the Game: Calls GameFactory.CreateGame() with the user's selections.
            

            // 4. Runs the Game: Calls gameInstance.RunGameLoop(). This is where all the actual gameplay


            // 5. Error Handling: Includes basic try-catch blocks for common issues


            // 6. Exits: After the RunGameLoop() finishes (meaning the game ended or the user quit from within the game), it prints a goodbye message and waits for a key press to close the console window.


        }
    }
}
